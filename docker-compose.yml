services:
  ml-app:
    image: ${DOCKERHUB_IMAGE:-yourdockerhubusername/equity-ml}:${IMAGE_TAG:-latest}
    ports:
      - "8501:8501"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
