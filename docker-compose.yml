services:
  ml-app:
    image: ${DOCKERHUB_IMAGE:-antsxd/machine_learning_cross-sectional_equity_return_predictor}:${IMAGE_TAG:-latest}
    ports:
      - "8501:8501"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
