services:
  ml-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: equity-ml-app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
